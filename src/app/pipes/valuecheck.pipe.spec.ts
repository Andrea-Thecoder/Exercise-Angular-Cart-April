import { ValuecheckPipe } from './valuecheck.pipe';

describe('ValuecheckPipe', () => {
  it('create an instance', () => {
    const pipe = new ValuecheckPipe();
    expect(pipe).toBeTruthy();
  });
});
